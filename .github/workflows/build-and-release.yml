name: Build and Release

# 此工作流在 GitHub Actions runner 上直接构建 Android APK
# 不依赖 EAS Build 云服务，节省免费构建次数

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      skip_build:
        description: 'Skip build (use [skip build] in commit message)'
        required: false
        default: 'false'

jobs:
  build-and-release:
    name: Build APK and Create Release
    runs-on: ubuntu-latest
    permissions:
      contents: write  # 需要写入权限来创建 Release
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 获取完整历史，某些操作可能需要

      - name: Check if build should be skipped
        id: check_skip
        run: |
          if [[ "${{ github.event.head_commit.message }}" == *"[skip build]"* ]] || [[ "${{ github.event.inputs.skip_build }}" == "true" ]]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "⏭️ Build skipped due to [skip build] in commit message or manual skip"
            exit 0
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      - name: Setup Node.js
        if: steps.check_skip.outputs.skip != 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        if: steps.check_skip.outputs.skip != 'true'
        run: npm ci

      - name: Setup Java
        if: steps.check_skip.outputs.skip != 'true'
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'  # 缓存 Gradle 依赖

      - name: Setup Android SDK
        if: steps.check_skip.outputs.skip != 'true'
        uses: android-actions/setup-android@v3

      - name: Cache Gradle dependencies
        if: steps.check_skip.outputs.skip != 'true'
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
            android/.gradle
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Cache Android build
        if: steps.check_skip.outputs.skip != 'true'
        uses: actions/cache@v4
        with:
          path: |
            android/app/build
            android/build
          key: ${{ runner.os }}-android-build-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-android-build-

      - name: Run expo prebuild
        if: steps.check_skip.outputs.skip != 'true'
        run: |
          echo "Generating native Android project..."
          
          # 检查 android 目录和 gradlew 文件是否存在
          NEED_PREBUILD=false
          
          if [ ! -d "android" ]; then
            echo "📦 android directory not found, need prebuild"
            NEED_PREBUILD=true
          elif [ ! -f "android/gradlew" ]; then
            echo "⚠️ gradlew not found, need prebuild"
            NEED_PREBUILD=true
          elif [ "package.json" -nt "android" ] || [ "app.json" -nt "android" ]; then
            echo "📦 package.json or app.json changed, need prebuild"
            NEED_PREBUILD=true
          else
            echo "✅ Android project exists and appears up to date"
          fi
          
          if [ "$NEED_PREBUILD" = "true" ]; then
            echo "📦 Running expo prebuild..."
            npx expo prebuild --platform android --clean
            
            # 验证 prebuild 结果
            if [ ! -d "android" ]; then
              echo "❌ Error: android directory not created after prebuild"
              exit 1
            fi
            
            if [ ! -f "android/gradlew" ]; then
              echo "❌ Error: gradlew not found after prebuild"
              echo "Listing android directory contents:"
              ls -la android/ || true
              echo ""
              echo "Checking for gradle wrapper:"
              find android -name "gradlew" -type f 2>/dev/null || echo "gradlew not found"
              exit 1
            fi
          fi
          
          # 最终验证
          if [ ! -f "android/gradlew" ]; then
            echo "❌ Error: gradlew still not found after all checks"
            exit 1
          fi
          
          echo "✅ Android project ready"
          echo "  gradlew path: android/gradlew"
          echo "  gradlew exists: $(test -f android/gradlew && echo 'yes' || echo 'no')"

      - name: Setup Android Keystore
        if: steps.check_skip.outputs.skip != 'true'
        id: setup_keystore
        run: |
          if [ -z "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" ]; then
            echo "⚠️ Warning: ANDROID_KEYSTORE_BASE64 not set, building unsigned APK"
            echo "unsigned=true" >> $GITHUB_OUTPUT
          else
            echo "Setting up signing keystore..."
            echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > android/qingbu-release-key.jks
            echo "unsigned=false" >> $GITHUB_OUTPUT
          fi

      - name: Configure Gradle Signing
        if: steps.check_skip.outputs.skip != 'true' && steps.setup_keystore.outputs.unsigned != 'true'
        run: |
          cd android
          cat >> app/build.gradle << EOF

          android {
              signingConfigs {
                  release {
                      storeFile file('../qingbu-release-key.jks')
                      storePassword '${{ secrets.ANDROID_KEYSTORE_PASSWORD }}'
                      keyAlias '${{ secrets.ANDROID_KEY_ALIAS }}'
                      keyPassword '${{ secrets.ANDROID_KEY_PASSWORD }}'
                  }
              }
              buildTypes {
                  release {
                      signingConfig signingConfigs.release
                  }
              }
          }
          EOF
          echo "✅ Signing configuration added"

      - name: Build APK
        if: steps.check_skip.outputs.skip != 'true'
        run: |
          # 验证 gradlew 存在
          if [ ! -f "android/gradlew" ]; then
            echo "❌ Error: gradlew not found"
            exit 1
          fi
          
          cd android
          echo "Building Release APK..."
          chmod +x gradlew
          
          # 验证 gradlew 可执行
          if [ ! -x "gradlew" ]; then
            echo "❌ Error: gradlew is not executable"
            exit 1
          fi
          
          # 使用构建缓存和并行构建加速
          ./gradlew assembleRelease --no-daemon --build-cache --parallel
          
          if [ ! -f "app/build/outputs/apk/release/app-release.apk" ]; then
            echo "❌ Error: APK not found after build"
            exit 1
          fi
          
          APK_SIZE=$(ls -lh app/build/outputs/apk/release/app-release.apk | awk '{print $5}')
          echo "✅ APK built successfully"
          echo "  Size: $APK_SIZE"
          echo "  Path: app/build/outputs/apk/release/app-release.apk"

      - name: Copy APK to workspace root
        if: steps.check_skip.outputs.skip != 'true'
        run: |
          cp android/app/build/outputs/apk/release/app-release.apk ./qingbu.apk
          APK_SIZE=$(ls -lh ./qingbu.apk | awk '{print $5}')
          echo "✅ APK copied to workspace root"
          echo "  File: qingbu.apk"
          echo "  Size: $APK_SIZE"

      - name: Read version from package.json
        if: steps.check_skip.outputs.skip != 'true'
        id: get_version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "📦 Detected version: $VERSION"

      - name: Get latest commit message
        if: steps.check_skip.outputs.skip != 'true'
        id: get_latest_commit
        run: |
          MESSAGE="${{ github.event.head_commit.message }}"
          echo "message<<'EOF'" >> $GITHUB_OUTPUT
          echo "$MESSAGE" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        if: steps.check_skip.outputs.skip != 'true'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.get_version.outputs.version }}-${{ github.sha }}
          name: Release v${{ steps.get_version.outputs.version }}
          body: |
            ## 轻簿 v${{ steps.get_version.outputs.version }}
            
            ### 构建信息
            - Commit: `${{ github.sha }}`
            - 构建时间: ${{ github.event.head_commit.timestamp }}
            - 构建方式: GitHub Actions 本地构建
            
            ### 下载
            点击下方下载 APK 文件进行安装。
            
            ### 变更说明
            
            ${{ steps.get_latest_commit.outputs.message }}
          files: |
            qingbu.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Release summary
        if: steps.check_skip.outputs.skip != 'true'
        run: |
          echo "✅ Release created successfully!"
          echo ""
          echo "📱 Release Information:"
          echo "  Version: v${{ steps.get_version.outputs.version }}"
          echo "  Tag: v${{ steps.get_version.outputs.version }}-${{ github.sha }}"
          echo "  APK: qingbu.apk"
          echo ""
          echo "🔗 View release: https://github.com/${{ github.repository }}/releases"
